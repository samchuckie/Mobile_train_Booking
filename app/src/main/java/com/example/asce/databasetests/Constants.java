package com.example.asce.databasetests;

class Constants {
    final static String REGEX = "[a-zA-Z0-9]+@[A-z]+(.[A-z])+";
    final static String NETWORK = "A network error (such as timeout, interrupted connection or unreachable host) has occurred.";
    final static String UNREGISTERED = "There is no user record corresponding to this identifier. The user may have been deleted.";
    final static String Consumer_key  = BuildConfig.Consumer_key;
    final static String Consumer_secret  = BuildConfig.Consumer_secret;
}
